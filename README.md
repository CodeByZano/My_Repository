# Machine Learning Example
Step 1: I provided the model with historical data since it does not know about education or math. It just sees two lists of numbers. 

Step 2: I create a blank model

Step 3: The machine starts learning. When the ".fit()" is called, the model plots the data on graph and uses calculus to minimize the distance between a straight line and the data points. Then it establishes that every 1 hour of study equals 10 points on a test starting from a base of 40.

Step 4: The model figures out the rule and starts predicting scores, applying the rule it generated in step 3.

Last part is the output
